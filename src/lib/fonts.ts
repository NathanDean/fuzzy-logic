import { Sigmar, Outfit } from 'next/font/google';

export const sigmar = Sigmar({
  weight: '400',
  subsets: ['latin'],
});

export const outfit = Outfit({
  weight: 'variable',
  subsets: ['latin'],
});
