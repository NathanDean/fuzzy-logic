import { Oi, Outfit } from "next/font/google";

export const oi = Oi({
    weight: "400",
    variable: "--font-oi",
    subsets: ["latin"]
});
  
export const outfit = Outfit({
    weight: "variable",
    subsets: ["latin"]
});